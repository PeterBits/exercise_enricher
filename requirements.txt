# Exercise Enricher Dependencies
# Install with: pip install -r requirements.txt

# AI Provider Libraries
anthropic>=0.39.0      # Claude (Anthropic) API client [PAID]
openai>=1.0.0          # OpenAI (GPT) API client [PAID]
groq>=0.4.0            # Groq API client [FREE] ⭐

# Environment variable management (for .env file support)
python-dotenv>=1.0.0
